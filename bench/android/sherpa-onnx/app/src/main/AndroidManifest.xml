<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:label="SherpaBench"
        android:theme="@android:style/Theme.Material.Light">
        <activity
            android:name=".BenchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- issue #130 Part 2 — launched explicitly via
             `am start -n com.aidedx.sherpabench/.DataGenActivity`, not the launcher icon
             (BenchActivity already owns that). -->
        <activity
            android:name=".DataGenActivity"
            android:exported="true" />
    </application>
</manifest>
