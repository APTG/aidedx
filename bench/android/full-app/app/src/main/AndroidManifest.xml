<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- issue #161 — only used by DownloadsExporter's legacy (API 26-28) export path; MediaStore's
         Downloads collection (API 29+, this app's actual test device) needs no permission at all.
         maxSdkVersion so it's a no-op — not even requested — on every modern device. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="false"
        android:label="AidedxFullApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <!-- issue #161 — this Activity holds live state a destroy/recreate would lose or
                 leak: an in-flight recording (AudioRecorder's mic stays open until stop() is
                 called), the ~639 MB loaded ASR model, and any in-progress download/transcription
                 Thread. Declaring configChanges keeps the Activity instance alive across a
                 rotation; MainActivity.onConfigurationChanged() re-inflates the view (picking
                 res/layout-land/ as needed) and reapplies the tracked UI state instead. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ModelManagerActivity"
            android:exported="false"
            android:label="Downloaded models" />
        <activity
            android:name=".CaptureManagerActivity"
            android:exported="false"
            android:label="Debug captures" />
    </application>
</manifest>
