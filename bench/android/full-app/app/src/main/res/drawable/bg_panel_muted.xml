<?xml version="1.0" encoding="utf-8"?>
<!-- issue #144 — rounded-lg muted panel (web: rounded-lg border border-input bg-muted, e.g. the
     transcribing-state background in MicButton.svelte / ModelDownloadBanner.svelte's shell). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/muted" />
    <stroke android:width="1dp" android:color="@color/input" />
    <corners android:radius="8dp" />
</shape>
