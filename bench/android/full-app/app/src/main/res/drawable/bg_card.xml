<?xml version="1.0" encoding="utf-8"?>
<!-- issue #144 — rounded-lg card/banner background (web: rounded-lg border border-input bg-card /
     bg-muted, e.g. ModelDownloadBanner.svelte). radius 8dp ~= CSS's radius-lg token (0.5rem). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/card" />
    <stroke android:width="1dp" android:color="@color/input" />
    <corners android:radius="8dp" />
</shape>
