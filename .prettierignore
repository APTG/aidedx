.svelte-kit
node_modules
build
coverage

# Local Python venvs + caches for GPU/TTS pilots (docs/athena-setup.md) — gitignored, but
# prettier doesn't read .gitignore, and these contain multi-GB binaries it chokes on
.venv-tts
.venv-qwen
.cache
.npm-global
.hf-cache-py
.hf-cache

# Lock files are managed by pnpm, not prettier
**/pnpm-lock.yaml

# Generated alias artifacts — owned by scripts/generate-aliases.ts (issue #4)
static/aliases/

# Vendored libdedx WASM artifacts — built by emscripten, not hand-maintained (issue #6)
static/wasm/
